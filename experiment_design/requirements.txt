# requirements.txt
# ----------------
# Python 3.10+ required
#
# Install:
#   pip install -r requirements.txt
#
# For vLLM on Lambda A100 (CUDA 12.1):
#   pip install vllm>=0.4.3
#
# Note: vllm is not listed here because it must be installed separately
# with the correct CUDA wheel for your environment. See:
#   https://docs.vllm.ai/en/latest/getting_started/installation.html

# -----------------------------------------------------------------------
# Core inference
# -----------------------------------------------------------------------
openai>=1.30.0             # vLLM OpenAI-compatible client
httpx>=0.27.0              # async HTTP (openai dep)

# -----------------------------------------------------------------------
# Analysis
# -----------------------------------------------------------------------
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0
statsmodels>=0.14.0        # multipletests (FDR correction)
matplotlib>=3.8.0
tabulate>=0.9.0            # pandas .to_markdown()

# -----------------------------------------------------------------------
# Configuration & utilities
# -----------------------------------------------------------------------
pyyaml>=6.0.1
tqdm>=4.66.0

# -----------------------------------------------------------------------
# Development / testing
# -----------------------------------------------------------------------
pytest>=8.0.0
pytest-cov>=5.0.0

# -----------------------------------------------------------------------
# Optional: Jupyter for interactive analysis
# -----------------------------------------------------------------------
# jupyter>=1.0.0
# ipykernel>=6.0.0
# seaborn>=0.13.0          # nicer defaults for EDA plots
